import { NatureCompteTitre } from './nature-compte-titre';

describe('NatureCompteTitre', () => {
  it('should create an instance', () => {
    expect(new NatureCompteTitre()).toBeTruthy();
  });
});
